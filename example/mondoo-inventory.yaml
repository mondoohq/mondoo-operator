apiVersion: v1
kind: ConfigMap
metadata:
  name: mondoo-inventory
data:
  inventory: |
    apiVersion: v1
    kind: Inventory
    metadata:
      name: mondoo-k8s-inventory
      labels:
        environment: production
    spec:
      assets:
        - id: host
          connections:
            - host: /mnt/host
              backend: fs
        - id: api
          connections:
            - backend: k8s