apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mondoo-operator.fullname" . }}-test-connection"
  labels:
    {{- include "mondoo-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mondoo-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
